
def build(bld):
    bld.shlib(target = 'gradient',
            source = 'gradient.cpp',
            use='EIGEN',
            export_includes = ['.'])
    bld.shlib(target = 'bfgs',
            source = 'bfgs.cpp',
            use='EIGEN',
            export_includes = ['.'])
