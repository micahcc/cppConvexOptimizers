
def build(bld):
    bld.shlib(target = 'opt',
            source = 'opt.cpp',
            use='EIGEN',
            export_includes = ['.'])
    bld.shlib(target = 'gradient',
            source = 'gradient.cpp',
            use='opt EIGEN',
            export_includes = ['.'])
    bld.shlib(target = 'bfgs',
            source = 'bfgs.cpp',
            use='opt EIGEN',
            export_includes = ['.'])
    bld.shlib(target = 'lbfgs',
            source = 'lbfgs.cpp',
            use='opt EIGEN',
            export_includes = ['.'])
